import pandas as pd

def createDataframe(student_data: List[List[int]]) -> pd.DataFrame:
    list_df = pd.DataFrame(student_data)

    list_df.rename(columns ={0:'student_id',1:'age'}, inplace=True)

    return list_df
